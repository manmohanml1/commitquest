# Milestone 0.4.0: User-owned saved campaigns

**Status:** Ready for owner review. No merge, production deployment, tag, or release is authorized.

## Outcome

A visitor can sign in with GitHub, save an ephemeral public-repository preview as a private campaign, return to it later, refresh it deliberately, change its stored visibility preference, export its data, delete it, sign out, and delete the account's imported data.

GitHub identity proves the account owner. It does not grant repository access. Private repositories and GitHub App installation remain v0.5 work.

## Acceptance criteria

- GitHub OAuth requests no scopes and uses one-time expiring state plus S256 PKCE.
- OAuth access tokens are discarded after CommitQuest reconciles the GitHub user identity.
- The browser receives an opaque Secure, HttpOnly, SameSite session cookie and no reusable provider credential.
- Sessions are revocable, expire, rotate on sign-in, and survive a Render restart through PostgreSQL.
- Every saved-campaign operation derives the owner from the authenticated session, never a request-supplied user identifier.
- Saving the same repository refreshes the existing campaign instead of creating duplicates.
- New campaigns are private by default; v0.4 may retain an unlisted preference but exposes no public showcase route.
- A campaign snapshot records projection, mapping, scoring, and export schema versions.
- List, read, refresh, visibility, export, and delete paths cannot reveal another account's campaign.
- Account deletion revokes sessions and deletes imported campaign data transactionally.
- The bundled campaign and ephemeral public preview continue working without authentication or PostgreSQL.
- Connected behavior remains disabled unless its required database and identity configuration is complete.
- PostgreSQL migrations, adapter integration, authorization matrices, OpenAPI generation, Java architecture checks, Angular behavior, and production builds pass.

## Delivery slices

### 1. Domain and decision foundation

- [x] Accept the identity, session, ownership, export, and deletion ADR.
- [x] Add provider-neutral account and saved-campaign domain types.
- [x] Enforce private defaults, owner-scoped lookup, refresh-in-place, export, deletion, and per-account limits in application tests.
- [x] Correct the mapping-version OpenAPI drift and add a regression check.

### 2. PostgreSQL persistence

- [x] Add Flyway, jOOQ, PostgreSQL, and Testcontainers only behind the connected-product configuration boundary.
- [x] Add forward-only account, OAuth state, session, and saved-campaign migrations.
- [x] Implement owner-scoped campaign persistence and database-cascade account deletion.
- [x] Prove the default database-free v0.3 application context still starts.

The five PostgreSQL 17 Testcontainers tests execute in the API CI job and passed on pull request #17. A Docker-free workstation may report them as skipped, but that local result is not used as persistence acceptance evidence.

### 3. GitHub identity and session transport

- [x] Implement GitHub OAuth authorization and callback endpoints with no requested scope and S256 PKCE.
- [x] Issue, rotate, validate, revoke, and expire opaque sessions while persisting only HMAC digests.
- [x] Enforce double-submit CSRF, Origin/Referer validation, and allowlisted relative return paths.
- [x] Add session, logout, and delete-account endpoints behind complete connected configuration.

### 4. Saved-campaign API and web experience

- [x] Add create, list, read, refresh, visibility, export, and delete HTTP contracts.
- [x] Generate and consume the Angular client.
- [x] Add accessible unavailable, signed-out, signing-in, saved, expired-session, export, and destructive-confirmation states.
- [x] Run desktop, mobile, fallback, database-disabled, and provider-failure journeys.

The database-disabled desktop journey, public-preview failure fallback, connected-unavailable fallback, and responsive browser journey pass. Angular regression tests cover signed-out, connected-unavailable, owner-library, successful-save, and guarded account-deletion states. Browser inspection at and below 390 CSS pixels confirms that navigation, hero copy, the campaign card, and the preview form remain within the viewport.

### Connected-environment readiness

- [x] Provision a dedicated Neon Free `commitquest-db` project with Neon Auth disabled.
- [x] Register a dedicated GitHub OAuth App with the exact production callback and no Device Flow.
- [x] Store database and identity settings as server-only Render environment variables.
- [x] Rotate and delete the initially exposed OAuth client secret.
- [ ] Complete owner review of pull request #17.
- [ ] Deploy the reviewed v0.4 artifact under separate owner authorization.
- [x] Run the signed-in save, return, refresh, visibility, export, delete, logout, and account-deletion journeys against an isolated connected preview.
- [x] Confirm mobile behavior and provider-failure fallback against preview artifacts.

The exact branch commit was exercised through an isolated Render Free API preview, the local same-origin Angular proxy, and the Vercel branch preview. The temporary Render service was deleted after QA and the OAuth callback was restored to production. Production configuration was corrected with **Save only** and production still runs the prior v0.3 implementation, so these results are review evidence rather than production-release evidence.

## Limits

- Maximum 25 saved campaigns per account in v0.4.
- One owner and one public GitHub repository per campaign.
- No background synchronization; refresh is explicit until v0.6.
- No public showcase response; unlisted/public presentation begins in v0.8.
- No private repository access, repository writes, billing, AI-authored state, queue, or paid resource.

## Release rule

Completing this checklist does not authorize a `v0.4.0` tag, GitHub Release, merge, or production deployment. The branch commit, push, draft pull request, isolated preview, Neon project, OAuth App, and provider-secret setup recorded above were separately approved; every remaining external action still requires explicit owner approval.

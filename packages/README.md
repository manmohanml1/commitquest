# Shared packages

This directory will contain generated API contracts, design tokens, and reusable campaign assets when a second consumer exists. The Angular application currently owns its fixture and domain projection locally to avoid speculative sharing.
